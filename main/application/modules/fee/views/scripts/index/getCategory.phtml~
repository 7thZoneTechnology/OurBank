<?php
$id=$_GET['selectedval'];
if (!empty($_GET['selectedval'])) {

echo $id;
$id=$_GET['selectedval'];


$dbobj = new Feecommon_Model_Feecommon();

$values = $dbobj->getcategory($id);

echo "$values";

print_r($values);
<!--$select=$this->select()

            ->setIntegrityCheck(false)
            ->join(array('a'=>'ourbank_category'),array('id'));
echo $id;-->
}
?> 